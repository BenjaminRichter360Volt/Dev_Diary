# Was machte ich gestern:

Gestern habe ich die Auth API und die Frontend-Test-App nach Azure hochgeladen und getestet.  
Dabei habe ich geprüft, dass Session und Token korrekt gespeichert werden und dass domainübergreifende Cookies funktionieren.  
Auch der Zugriff auf die Google API, zum Beispiel für das Profil, läuft.  
Zusätzlich habe ich das Mitarbeiterportal lokal mit der Auth API verbunden und Login, Logout sowie die Kalender-Seite und das Widget gefixt.  
Das Frontend sollte nun ohne Access Token und Local Storage funktionieren.
# Was will ich heute machen:
Heute möchte ich die Auth API mit SSL ausstatten, damit das Team das Image jederzeit ziehen und lokal nutzen kann.  
Außerdem würde ich gerne besprechen, was im Mitarbeiterportal noch angepasst werden muss und ob wir weitere Google-Funktionen brauchen, um alles zentral zu halten und keine sensiblen Tokens oder Daten im Frontend zu haben.  
Als Idee würde ich auch vorschlagen, einen Auth Context bzw. eine zentrale Komponente als eigenes privates NPM-Paket zu bauen, damit alle Teams auf dem gleichen Stand sind.
# Was habe ich geschafft:
- Docker Image SSL
- Projekt Migrate Fehler behoben und alten Stand mit Directus wieder eingebaut

# Womit gab es Probleme:

# Speichern für Später:
